# ─────────────────────────────────────────────────────────────────
# Portfolio Website – Environment Variables Reference
# Copy the values you need into Supabase → Settings → Edge Functions
# NEVER commit real secrets to git.
# ─────────────────────────────────────────────────────────────────

# Supabase (already configured by the platform, no action needed)
SUPABASE_URL=https://<your-project-ref>.supabase.co
SUPABASE_ANON_KEY=<your-anon-key>
SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>

# ── Resend (email notifications for contact form) ─────────────────
# 1. Go to https://resend.com and create a free account
# 2. Dashboard → API Keys → Create API Key
# 3. Copy the key and add it in Supabase:
#    Supabase Dashboard → Settings → Edge Functions → Secrets
#    Name : RESEND_API_KEY
#    Value: re_xxxxxxxxxxxxxxxxxxxx
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
